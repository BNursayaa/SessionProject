# Frontend (Next.js Dashboard)

## Run

```powershell
npm install
npm run dev
```

Env:

- `NEXT_PUBLIC_API_BASE` (default UI expects `http://localhost:8000`)
- `NEXT_PUBLIC_WS_URL` (default `ws://localhost:8000/ws`)

